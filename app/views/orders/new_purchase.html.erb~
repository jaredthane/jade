<% content_for :title do %>Nuevo Pedido<% end %>

<%= error_messages_for :order %>

<% form_for(@order, :url => order_path(@order)) do |f| %>
	  <%= render :partial => 'fields', :locals => { :f => f } %>
  
  <p>
    <%= f.submit "Nuevo" %>
  </p>
<% end %>

<%= link_to 'Cancelar', orders_path, :class => 'action' %>
