<% content_for :title do %>Lista de Pedidos<% end %>

<%= error_messages_for :order %>

<% form_for(@order) do |f| %>
  <p>
    <b>Vendedor</b><br />
    <%= f.collection_select(:vendor_id, get_vendors, :id, :name) %>
  </p>

  <p>
    <b>Cliente</b><br />
    <%= f.collection_select(:client_id, get_clients, :id, :name) %>
  </p>

  <p>
    <b>Fecha de Pedido</b><br />
    <%= f.date_select :ordered %>
  </p>

  <p>
    <b>Usuario Responsable</b><br />
    <%= f.collection_select(:user_id, get_users, :id, :name) %>
  </p>

  <p>
    <%= f.submit button_text %>
  </p>
<% end %>
<% if mode == 'edit' %>
	<%= link_to 'Ver', @order %> |
	<%= link_to 'Remover', @entity, :confirm => 'Esta seguro?', :method => :delete, :class => 'action' %> |
<% end %>
<%= link_to 'Atras', orders_path %>
