<% content_for :title do %>Cambiar Cuenta Fisica #<%= @order.id %><% end %>
<%form_for @order, :url => {:action => 'update', :controller => 'orders'}, :html => {:id => 'counts_form' } do |f|  %>
	<%= render :partial => 'counts/fields', :locals => { :order_type_id => @order.order_type_id, :f => f} %>
<% end %>
<br />
<%= render :partial => 'new_line_form', :locals => {:order => @order,:order_type_id => @order_type_id} %>
