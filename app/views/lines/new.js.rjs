page.replace_html 'lines_errors', ""
if @order_type_id == '5' 
	page.insert_html :bottom, :lines, :partial => 'physical_counts/line', :collection => @lines
else
	page.insert_html :bottom, :lines, :partial => 'orders/line', :collection => @lines
end
page.select(".serial").last.focus()
