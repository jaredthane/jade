
<% content_for :title do %><%= heading %><% end %>
<% content_for :actions do %>
	<% if mode=='edit' %>
		<dt><%= link_to 'Remover', @unit, :confirm => 'Esta seguro?', :method => :delete, :class => 'action' %></dt>
	<% end %>
	<dt><%= link_to 'Cancelar', units_path, :class => 'action' %></dt>
<% end %>

<%= error_messages_for :unit %>

<% form_for(@unit) do |f| %>
  <p>
    <b>Nombre</b><br />
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.submit button_text, :class => 'action' %>
  </p>
<% end %>


