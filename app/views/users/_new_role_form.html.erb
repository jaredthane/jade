<% form_remote_for(RolesUser.new(), :url => new_user_role_path) do |f| %>
	
	<%= f.collection_select(:role_id, get_roles, :id, :title) %>
	
  <%= link_to_function "Remover", 'get_new_role' %>
<% end %>

