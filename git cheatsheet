Create named repo
git remote add origin git@github.com:jaredthane/jade.git

Create local repo (First time install)
git clone git://github.com/jaredthane/jade.git

Add new files to git
git add .
  -OR-
git add filename

To save changes to local repo
sudo git commit -a -m 'description of changes'

To copy local repo to github
git push origin master

To update local copy to match github
git pull

To change to branch
git checkout unstable

To create and change to branch
git checkout -b unstable

To merge changes made in a branch into the master:
git checkout master
git merge unstable

To update a branch to match the master
git checkout unstable
git merge master

To Remove file from disk and git
git rm filename



